// Ensure calculateAll function is loaded from 2222.js before this runs

// Run the calculation once on page load
calculateAll();

// Re-run the calculation every second to update the time
setInterval(calculateAll, 1000);
